# Session — 2026-04-12

## Decisions
- Compound Node System: dokumentiert als Phase 7 in WVS-PLAN.md — nicht implementieren jetzt
- Quad Sphere ersetzt UV-Sphere komplett (kein zweiter Typ); Icosphere bleibt als Option erhalten
- Sphere: BoxGeometry → inflate → lat/lon UV (1 Naht, keine Pol-Singularität). Pol-Konvergenz unter Displacement ist prinzipbedingt unlösbar mit UV-basiertem Displacement
- Texture Normal Source-Port: immer sichtbar + auto-switch Mode beim Verbinden (kein manuelles Mode-setzen nötig)
- `geometry/gltf-mesh` aus Palette versteckt (hidden: true), umbenannt zu "glTF Geometry"
- `NodeDefinition.hidden` Flag eingeführt für interne Nodes

## What was implemented
- **Quad Sphere**: BoxGeometry → toNonIndexed() → inflate → lat/lon UV + longitude seam fix. Normals = normalize(position). Seam-fix Algorithmus: bump low-u vertices by +1 in seam-straddling triangles.
- **Texture Normal fix**: `source` port immer sichtbar; onConnect auto-switch zu mode=2
- **glTF Expand — Mesh-Positionen**: `updateMatrixWorld(true)` vor Traversal; matrixWorld (16 floats) in gltf-mesh node data gespeichert; `geo.applyMatrix4()` beim Klonen in SceneRenderer
- **glTF Expand — Transparenz**: `transparent`, `opacity`, `transmission` (MeshPhysicalMaterial), `side` aus Material extrahiert
- **glTF Expand — Folder-Import**: `extraFiles` durch gesamte Pipeline (expand → compiler → geometrySource → loadGltfGeometries) — LoadingManager mit URLModifier für .bin + Texturen
- **glTF Geometry Node**: hidden: true, umbenannt, aus Palette gefiltert
- **Selection Highlight**: orange Wireframe jetzt auch für gltf-mesh Geometrie

## Open items
- Sphere Pol-Konvergenz unter Displacement (prinzipiell: braucht Object-Space Displacement via Custom GLSL)
- Custom GLSL Shader-Node (Phase 5d Rest)
- FBX/OBJ Import (Phase 5e Fortsetzung)

## Next concrete steps
1. Custom GLSL Shader-Node (TextAreaControl + ShaderMaterial)
2. FBX Import via THREE.js FBXLoader (selbe Expand-Architektur wie glTF)
