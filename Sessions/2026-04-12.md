# Session — 2026-04-12

## Decisions

- **shader/colorramp** implementiert: Multi-Stop Gradient sfloat→svec3, `ColorRampControl` mit click-to-add-stop (Gradient-Leiste) + Inline Color Picker
- **shader/domainwarp** implementiert: Inigo Quilez Domain Warp (svec3→svec3), 3× fBm mit IQ-Offsets
- **shader/component** implementiert: vec3→sfloat Extraktor (X/Y/Z), default Y — nötig für Stripe/Ring-Effekte
- **shader/band** implementiert: Schmales Band-Pattern mit smoothstep (T, Center, Width, Sharpness) — für Scan-Line/Ring-Effekte
- **Worley + Curl** zu shader/noise hinzugefügt (op=3, op=4); Detail-Property nur für fBm/Ridged/Curl sichtbar
- **Lerp (op=7) + Fract (op=8)** zu shader/math hinzugefügt; Lerp mit drittem T-Port
- **NodeRenderer** verbessert: fallbackSelectProp zeigt aktive Operation im Header ("Shader Math: Fract")
- **ColorRampControl** Pattern: Click auf Bar → Stop hinzufügen; Rechtsklick → löschen; min. 2 Stops

## Open Items

- **Scan-Line Bounce-Bug**: Ring fährt noch zurück statt sofort-Reset — Subtract A/B evtl. vertauscht. Vorgeschlagener Fix: `Multiply(fract, −radius*2) + Add(radius)` statt Subtract (Add ist kommutativ)
- FBX Import (Phase 5e) noch ausstehend
- Evtl. Step/Smoothstep/Remap als weitere Math-Ops

## Next Concrete Steps

1. Scan-Line Bug klären: Nutzer soll `Add statt Subtract` versuchen mit Shader Number `−4` (für Radius 2)
2. Oder: FBX Import angehen (Phase 5e)
3. `/commit` für diese Session
