# Session — 2026-04-12

## Decisions

- **Visual Shader Graph System vollständig implementiert** (Plan aus plan-file abgearbeitet)
  - Nodes: shader/uv, time, mouse, position, noise (fBm/Ridged/Voronoi), gradient, mix, math, color, number, output
  - 3 Rendering-Modes: Background (→ Scene Output), Unlit (→ Mesh), PBR (→ Material → Mesh)
  - PBR-Mode: `onBeforeCompile` mit `vertexPreamble` + `vertexBodyForPBR` Injection
  - Displacement via `displacement: sfloat` Input auf shader/output
  - Finite-Difference-Normals: eps=0.1, gradient clamp ±1.5, `_sg_disp(vec3 _sg_p)` Helper

- **Systemischer `select`-Bug gefixt**: SelectControl schreibt Strings ('0','1','2'), aber Code verglich mit `=== 0/1/2` → immer false. Fix: `Number(props.X ?? 0)` überall (noiseType, op, texMode, camMode, time.mode, math.mode, input.mode)

- **Three.js Program-Cache-Bug gefixt**: `customProgramCacheKey` war nur `vertexBodyForPBR` → Noise-Type-Wechsel triggerte kein Shader-Recompile weil nur `vertexPreamble` sich ändert. Fix: Key = preamble + body

- **shader/position Node**: object-space 3D position als Noise-Input → keine UV-Seam auf Meshes

- **Geometry-Limits festgelegt**:
  - Sphere/Plane: max 256 Segmente (512 laggy)
  - Icosphere: max 256 detail — IcosahedronGeometry `detail` ist LINEAR (20×(detail+1)² Dreiecke), nicht exponentiell. detail=80 ≈ 256-Sphere. detail=200 flüssig, detail=500 laggy

## Open Items

- PBR-Mode: Shader Graph Farbe (color output von shader/output) beeinflusst nicht das Material — nur Displacement. Wenn User shader-basierten Albedo will, braucht es neuen Feature-Pfad
- Finite-Diff-Normals bei hochfrequentem Voronoi noch nicht perfekt (prinzipielle Limitation)
- FBX/OBJ/STEP Import (Phase 5e) noch ausstehend

## Next Concrete Steps

1. FBX Import (Phase 5e) — oder
2. Shader Graph: color-output im PBR-Mode als Albedo (neuer Feature-Pfad)
3. Ggf. `/pre-compact` + commit
