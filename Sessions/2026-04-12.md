# Session — 2026-04-12

## Decisions
- Compound Node system (nested subgraphs): documented as Phase 7 in WVS-PLAN.md — NOT implementing yet
- glTF Import Node "Expand to Graph": implemented on current graph level (no subgraph needed)
- Expansion is destructive (removes original node) → use `window.confirm()` before proceeding
- UV fix approach: store `flipY: false` in extracted texture node data + apply in SceneRenderer — avoids canvas pre-flip which was unreliable for multi-mesh models
- FBX/OBJ/STEP formats deferred; architecture (gltf-expand.ts pattern) anticipates them

## What was implemented
- `geometry/gltf-mesh` node type (category: geometry, output: geometry port)
- `src/graph-engine/gltf-expand.ts` — `expandGLTFToNodes()`: loads glTF, traverses meshes, generates texture/material/geometry/mesh nodes + edges
- `CompiledMesh.geometrySource?: { type: 'gltf'; dataUrl: string; meshIndex: number }` in compiler
- `gltfGeometryCache` + `gltfGeometryLoading` module-level Maps in SceneRenderer for dedup loading
- MeshObject: loads geometry by index from cache, clones per node, disposes on unmount
- Texture `flipY: false` propagated through compiler's `resolveTextureSlot` → SceneRenderer texture loader
- PropertiesPanel: "Expand to Graph" button with confirm dialog + error display
- NodeRenderer: image preview thumbnail (`max-h-16`) + filename for texture/image nodes
- WVS-PLAN.md: Phase 5e block + Phase 7 Compound Node System block + Längerfristig table entry

## Open items
- Multi-mesh UV fix: needs verification with a complex model (not confirmed by Daniel yet)
- `geometry/gltf-mesh` selection highlight (orange wireframe) currently skipped — TODO
- Custom GLSL Shader-Node (Phase 5d remainder: TextAreaControl + ShaderMaterial)
- FBX import (Phase 5e continuation)
- OBJ import (Phase 5e continuation)

## Next concrete steps
1. Verify multi-mesh UV fix with a real complex GLB file
2. Implement Custom GLSL Shader-Node (TextAreaControl, ShaderMaterial)
3. FBX format support via THREE.js FBXLoader (same expand pattern)
