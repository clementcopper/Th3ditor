# Session — 2026-04-10

## Decisions
- `centerMode` select (Off / BBox Center / BBox Bottom / Manual) replaces old `centerOrigin: bool`
- Raw bbox must be computed BEFORE `groupRef.current.add(scene)` — world-matrix contamination otherwise
- Pivot TC removed; origin is set via sliders only (Manual mode), not draggable gizmo
- Origin indicator: fixed-pixel screen-space cross (`pointsMaterial` + canvas texture, `sizeAttenuation=false`, 14px, red `#ff3300`)
- No toggle for origin cross — always visible in editor view
- All UI text must be in English (added to CLAUDE.md)
- Phase 5b complete ✅

## Open items
- Camera Look-Ahead bounce on rotated circle paths — deferred (10+ fix attempts, see WVS-PLAN.md)

## Next concrete steps
- Phase 5c: Color-Node → Color-Ops → Custom GLSL Shader-Node
